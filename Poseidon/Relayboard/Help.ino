/*
 * this is a describtion on how to controll the relay board
 * 
 * Strukture is build like a sensor/ic all settings and readings are done via registre
 * set the registre pointer to a place in the registre do a read or write, registre pointer will increment 1 place after a reading or writing.
 * 
 */
